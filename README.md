# vscode-amiga-assembly-examples-packager
Tools to help packaging the Amiga Assembly Examples

Command to run :

`./gradlew clean zipBinaries`

The result files are in **build/distributions** directory.